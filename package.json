{
  "name": "serverless-employee-portal",
  "version": "1.0.0",
  "description": "serverless certificate creation and editing the values",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sagar.yelgond@hyniva.com",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.423.0",
    "@aws-sdk/util-dynamodb": "^3.423.0",
    "aws-sdk": "^2.1468.0",
    "serverless-iam-roles-per-function": "^3.2.0"
  }
}
